apiVersion: v1
kind: ConfigMap
metadata:
  name: scaffold-config
  namespace: default
  labels:
    app.kubernetes.io/name: knapscen-scaffold-database
    app.kubernetes.io/component: config
data:
  DB_HOST: "my-test-cluster.default.svc.cluster.local"  # Change this to your MySQL service name or external host
  DB_PORT: "3306"
  DB_NAME: "bassline-boogie"    # Change this to your target database name
